# @generated by autocargo from //resctl/below/cgroupfs:cgroupfs

[package]
name = "cgroupfs"
version = "0.11.0"
authors = ["Meta Platforms, Inc. and affiliates"]
edition = "2024"
description = "A crate for reading cgroupv2 data"
readme = "README"
repository = "https://github.com/facebookincubator/below"
license = "Apache-2.0"

[dependencies]
common = { package = "below-common", version = "0.11.0", path = "../common" }
libbpf-rs = { version = "=0.26.0-beta.0", default-features = false }
nix = { version = "0.30.1", features = ["dir", "event", "hostname", "inotify", "ioctl", "mman", "mount", "net", "poll", "ptrace", "reboot", "resource", "sched", "signal", "term", "time", "user", "zerocopy"] }
openat = "0.1.21"
serde = { version = "1.0.219", features = ["derive", "rc"] }
thiserror = "2.0.12"

[build-dependencies]
libbpf-cargo = { version = "=0.26.0-beta.0", default-features = false }
bindgen = "0.70.1"

[dev-dependencies]
paste = "1.0.14"
tempfile = "3.22"
